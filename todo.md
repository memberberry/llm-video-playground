## open points

* ändere die history tabelle damit sie keine id sondern einen hash als unique key hat. Es soll im frontend nur noch eine overview mit paar meta daten angezeigt werden. Wie zum beispiel dem hash, Erstellungsdatum und model ob es struct oder text ist und evtl. welche videos. Das anklicken zeigt dann die genauen prompts und responses.
* Videos panel im frontend muss angepasst werden damit das Bild auch angezeigt wird, mir fehlen da noch paar details
* passe den gemini models endpunkt an damit die model strings nicht mehr "models/" also prefix haben.
* Neue Endpunkte alle einmal testen.
* logging